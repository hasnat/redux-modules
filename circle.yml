machine:
  node:
    version: 5
deployment:
  production:
    branch: master
    commands:
      - npm publish
  staging:
    branch: /^(?!master\/.*).*$/
    commands:
      - npm publish --tag $CIRCLE_BRANCH
